name: ilahi_app
description: Ilahi uygulamasi

publish_to: 'none'

version: 1.1.0+2

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  just_audio: ^0.9.46
  audio_session: ^0.1.21
  url_launcher: ^6.2.5
  share_plus: ^10.0.2
  shared_preferences: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true

  assets:
    - assets/audio/
    - assets/images/
    - assets/text/
    - assets/text2/
    - assets/text3/
